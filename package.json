{
  "name": "ecommerce-platform",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "build": "npm install --prefix frontend --no-audit --no-fund && npm run build --prefix frontend && rm -rf dist && mkdir -p dist && cp -R frontend/dist/. dist/",
    "start": "npm start --prefix backend"
  }
}
